class CompilerStarter {
    float loadTime;
};

class CompilerPackage: CompilerStarter {
    float computationTime;
    float loadTime;
};

class Compiler: CompilerPackage, TestKit {
    float tested;
    integer test();
};

float Compiler::test(float t) {
    tested = t;
    return (12.2);
};

main {
    Compiler comp;
    integer comp = 12;
    integer result = comp.test(12);
};